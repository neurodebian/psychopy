--- a/setup.py
+++ b/setup.py
@@ -40,7 +40,7 @@ if PY3:  # doesn't exist on py2
 
 
 # compress psychojs to a zip file for packaging
-if '-noJS' in argv:  # only takes 0.5s but could skip if you prefer
+if '-noJS' in argv or 'clean' in argv:  # only takes 0.5s but could skip if you prefer
     pass
 else:
     import shutil
